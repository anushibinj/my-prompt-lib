spring:
  application:
    name: my-prompt-lib
  datasource:
    url: ${JDBC_URL:jdbc:postgresql://localhost:5432/promptdb}
    username: ${JDBC_USERNAME:postgres}
    password: ${JDBC_PASSWORD:password}
    driver-class-name: org.postgresql.Driver
  flyway:
    table: mpl_schema_table
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

google:
  client-id: ${GOOGLE_CLIENT_ID:your-google-client-id-here}

# To show warnings and info
logging:
  level:
    root: INFO
    com.anushibinj.mypromptlib: DEBUG
